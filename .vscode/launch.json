{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "codelldb",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "terminal": "integrated"
      // "preLaunchTask": "CMake: build"
    }
  ]
}
